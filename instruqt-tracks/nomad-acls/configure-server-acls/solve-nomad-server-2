#!/bin/bash -l

# Check the ACL Enabled
cat <<EOF >> /etc/nomad.d/server2.hcl

# ACL Enabled

acl {
    enabled = true
}
EOF

# Check Authoritive region
ex -s -c '13i|  authoritative_region = "global"' -c x /etc/nomad.d/server2.hcl

exit 0